import 'package:flutter/material.dart';

class AppColors {
  static const bgColor = Color.fromARGB(255, 0, 0, 0);
  static const blue = Color.fromARGB(255, 0, 140, 255);
  static const white = Colors.white;
  static const bgHome = Color(0xe4e4e4e4);
}